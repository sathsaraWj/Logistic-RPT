"""Repo-local dev/demo scripts — not part of the distributed `hermes_rpt` package. Safe to
import `tests.*` helpers from here (see `build_synthetic_dataset.py`); `src/hermes_rpt` and
`apps` must never do the same.
"""
